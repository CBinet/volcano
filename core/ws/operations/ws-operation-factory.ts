import { WsOperation } from './ws-operation';

export class WsOperationFactory {

    static createOperation(operation: WsOperation, request: Request, response: Response) {
        
        try {
           
        }
        catch (error) {
            
        }
    }

}